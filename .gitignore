# Rust
/target
**/*.rs.bk
Cargo.lock

# Installer build output
/dist/

# Java / Maven / Gradle
**/target/
**/build/
*.class
*.jar
!**/gradle-wrapper.jar
!tools/protege-plugin/build-and-install.bat
.gradle/

# IDE
.vscode/
.idea/
*.iml
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Database data directories (binary, not versioned)
loka-data/
loka-data-backup-*/
loka-data-cron-*/
loka-import-data/
loka-retrieval-data/
sutra-data/
sutra-data-backup-*/
sutra-import-data/
*.sdb
*.ldb

# BFS / HF import state (regenerated each run)
wikidata_import_state.json*
wikidata_hf_import_state.json*

# Inference / debugging artifacts
smoke_out.nt
claim_dump.json
hf_schema*.json
hf_schema*.txt
training/data/generated_*.nt

# Claude artifacts
mnt/
loka_error.log
*.pyc

# Throwaway probe scripts (underscore-prefixed = scratch, not tracked)
tools/_*.py

# Chat exports — commit only the extracted .md, not the bulky HTML and asset bundles
chats/*.html
chats/*_files/
chats/*-raw.md

# Fine-tune track — heavy/derived artifacts (corpus, JSONL, adapters, base cache)
training/finetune/data/
training/finetune/adapters/
training/finetune/logs/
loka-retrieval-data-stale-20260520/snap.000000002968C9AF
loka-retrieval-data-stale-20260520/db

# Local benchmark/stress output artifacts — regenerated by stress_test.py,
# tools/benchmark.py, tools/storage_benchmark.py. Live results live in benchmarks/.
/benchmark_results.json
/storage_benchmark_results.json
/stress_test_report.json
